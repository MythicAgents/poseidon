function(task, responses){  if(responses.length === 2){    try{        var status = JSON.parse(responses[0]['response']);    }catch(error){    	console.log(error)       return "<pre>" + JSON.stringify(responses, null, 2) + "</pre>";    }    if(status.hasOwnProperty('id')){      return "<div class='card'><div class='card-header border border-dark shadow'>Finished Downloading <span class='display'>" + escapeHTML(status['filename']) + "</span>. Click <a href='/api/v1.4/files/download/" + status['agent_file_id'] + "'>here</a> to download</div></div>";    }  }  return "<pre>" + responses[0]['response'] + "</pre>";}